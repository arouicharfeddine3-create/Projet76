name: Build Android APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      # بناء التطبيق مع تحديد إصدار Gradle صراحة
      - name: Build APK
        uses: gradle/actions/setup-gradle@v3
        with:
          gradle-version: '8.5'          # <-- هام جداً
          arguments: assembleDebug

      # تشغيل الاختبارات بنفس الإصدار
      - name: Run Tests
        uses: gradle/actions/setup-gradle@v3
        with:
          gradle-version: '8.5'
          arguments: test

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: app-debug
          path: app/build/outputs/apk/debug/*.apk
